{
  "name": "football-director-simulation",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A polished browser-based football club management simulation.",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "test": "node --test",
    "check": "node scripts/quality.mjs",
    "smoke": "node scripts/smoke-server.mjs",
    "verify": "npm test && npm run check && npm run smoke",
    "build": "node scripts/build.mjs",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name football-director-sim"
  },
  "engines": {
    "node": ">=22"
  }
}
